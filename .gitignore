node_modules/
dist/
coverage/
*.log
*.tgz

# keel proof-gate local state (receipts, verify command) — never commit
.keel/

# Local MCP config and registry secrets (never commit)
.mcp.local.json
.mcpregistry_*
